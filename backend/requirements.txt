# FastAPI framework and ASGI server (includes WebSocket support)
fastapi==0.120.1
uvicorn[standard]==0.38.0

# Database ORM (works with SQLite via Python's built-in sqlite3 module)
sqlalchemy==2.0.44

# Environment configuration
python-dotenv==1.2.1

# Data validation with email support
pydantic[email]==2.12.3

# Note: WebSocket support is included in FastAPI
# Note: SQLite support is built into Python's standard library

